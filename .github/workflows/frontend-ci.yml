# ══════════════════════════════════════════════════════════════
# InvoiceAZ Frontend CI Pipeline
# React (Vite) — Lint, Build, Security
# ══════════════════════════════════════════════════════════════
name: "⚛️ Frontend CI"

on:
  push:
    branches: [main, staging, develop]
    paths:
      - "frontend/**"
      - ".github/workflows/frontend-ci.yml"
  pull_request:
    branches: [main, staging]
    paths:
      - "frontend/**"

jobs:
  # ────────────────────────────────────────────────────────────
  # JOB 1: Lint + Build
  # ────────────────────────────────────────────────────────────
  lint-and-build:
    name: "🔍 Lint & Build"
    runs-on: ubuntu-latest

    steps:
      - name: "📥 Kodu yüklə"
        uses: actions/checkout@v4

      - name: "📗 Node.js qur"
        uses: actions/setup-node@v4
        with:
          node-version: "20"
          cache: "npm"
          cache-dependency-path: frontend/package-lock.json

      - name: "📦 Asılılıqları qur"
        run: |
          cd frontend
          npm ci

      - name: "🔍 ESLint ilə yoxla"
        run: |
          cd frontend
          npm run lint

      - name: "🏗️ Production build"
        run: |
          cd frontend
          npm run build

      - name: "📊 Build ölçüsü"
        run: |
          echo "📦 Build ölçüsü:"
          du -sh frontend/dist/
          echo ""
          echo "📂 Əsas fayllar:"
          find frontend/dist -type f \( -name "*.js" -o -name "*.css" \) | head -20

  # ────────────────────────────────────────────────────────────
  # JOB 2: Təhlükəsizlik
  # ────────────────────────────────────────────────────────────
  security:
    name: "🛡️ Təhlükəsizlik"
    runs-on: ubuntu-latest

    steps:
      - name: "📥 Kodu yüklə"
        uses: actions/checkout@v4

      - name: "📗 Node.js qur"
        uses: actions/setup-node@v4
        with:
          node-version: "20"

      - name: "🛡️ npm audit"
        run: |
          cd frontend
          npm audit --audit-level=high || true
